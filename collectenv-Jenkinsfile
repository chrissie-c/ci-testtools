
pipeline {
    agent {
        label "built-in"
    }
        
    options {
        timestamps()
    }
        
    stages {
        stage('agents-tasks') {
            
            steps {
                script {
		    collectBuildEnv = collectenv('rh')
      
                    parallel collectBuildEnv
                }
            }
        }
    }
}
