pipeline {
    agent {
	label 'fedora37'
    }
    stages {
	stage('Build and test') {
	    stages {
                stage('Prep') {
                    steps {
			sh 'export PKG_CONFIG_PATH=/srv/knet/origin/main/lib/pkgconfig/'
                        sh 'sh autogen.sh'
                        sh './configure'
                    }
                }
                stage('Build') {
                    steps {
                        sh 'echo $THISISSTRING'
			sh 'make all"
                    }
                }
                stage('Test') {
                    steps {
			sh 'make check'
                    }
                }
                stage('Dist Check') {
                    steps {
			sh 'make distcheck'
                    }
                }
            }
	}
    }
}
