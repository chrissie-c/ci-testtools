
def runStuff(Map info)
{
    println("${env.NODE_NAME}")
}

def buildRun(Map info)
{
    def runmap = [:]
    def allnodes = getAllNodes()
    for (i in allnodes) {
	runmap[i] = {
	    runStuff(info)
	}
    }
    return runmap
}

pipeline {
    agent { label 'built-in' }

    stages {
	// This is the main stage that covers everything
	stage('go') {
	    steps {
		script {
		    info = [:]
		    def map = buildRun(info)
		    println(map)
		    parallel map
		}
	    }
	}
    }
}

