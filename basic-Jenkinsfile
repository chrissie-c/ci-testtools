
pipeline {
    agent { label 'built-in' }

    stages {
	// This is the main stage that covers everything
	stage('go') {
	    steps {
		script {
		    def info = [:]
		    def map = buildRun(info)
		    println(map)
		    parallel map
		}
	    }
	}
    }
}

